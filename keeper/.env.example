# ─── Solana ───────────────────────────────────────────────────────────────────
# RPC endpoint (use a dedicated provider like Helius for reliable rate limits)
RPC_URL=https://api.devnet.solana.com
# Protocol admin keypair (signs oracle updates)
ANCHOR_WALLET=/path/to/id.json

# ─── Keeper settings (optional, defaults shown) ──────────────────────────────
#UPDATE_INTERVAL_MS=6500
#PRICE_VOLATILITY=0.006
#WL500_TARGET=5000
#API_PORT=3001
#MANIFEST_PATH=../app/src/lib/markets.bootstrap.json
